---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: may-app-client
  namespace: benchmark
  labels:
    prometheus: main
spec:
  namespaceSelector:
    matchNames:
    - benchmark
  selector:
    matchLabels:
      app: may-app-client
  podMetricsEndpoints:
  - port: metrics
