---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: actix-app-client
  namespace: benchmark
  labels:
    prometheus: main
spec:
  namespaceSelector:
    matchNames:
    - benchmark
  selector:
    matchLabels:
      app: actix-app-client
  podMetricsEndpoints:
  - port: metrics
