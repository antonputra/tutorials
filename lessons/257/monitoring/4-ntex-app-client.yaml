---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: ntex-app-client
  namespace: benchmark
  labels:
    prometheus: main
spec:
  namespaceSelector:
    matchNames:
    - benchmark
  selector:
    matchLabels:
      app: ntex-app-client
  podMetricsEndpoints:
  - port: metrics
