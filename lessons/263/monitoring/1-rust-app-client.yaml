---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: rust-app-client
  namespace: benchmark
  labels:
    prometheus: main
spec:
  namespaceSelector:
    matchNames:
    - benchmark
  selector:
    matchLabels:
      app: rust-app-client
  podMetricsEndpoints:
  - port: metrics
