---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: zig-client
  namespace: benchmark
  labels:
    prometheus: main
spec:
  namespaceSelector:
    matchNames:
    - benchmark
  selector:
    matchLabels:
      app: zig-client
  podMetricsEndpoints:
  - port: metrics
