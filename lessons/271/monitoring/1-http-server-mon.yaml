---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: http-client
  namespace: benchmark
  labels:
    prometheus: main
spec:
  namespaceSelector:
    matchNames:
    - benchmark
  selector:
    matchLabels:
      app: http-client
  podMetricsEndpoints:
  - port: metrics
