---
apiVersion: v1
kind: Service
metadata:
  name: http-server
  namespace: benchmark
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    app: http-server
