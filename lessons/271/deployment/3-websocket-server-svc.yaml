---
apiVersion: v1
kind: Service
metadata:
  name: websocket-server
  namespace: benchmark
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081
  selector:
    app: websocket-server
