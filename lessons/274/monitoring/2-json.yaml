---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: json-server
  namespace: benchmark
  labels:
    prometheus: main
spec:
  namespaceSelector:
    matchNames:
    - benchmark
  selector:
    matchLabels:
      app: json-server
  podMetricsEndpoints:
  - port: metrics
