---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: protobuf-server
  namespace: benchmark
  labels:
    prometheus: main
spec:
  namespaceSelector:
    matchNames:
    - benchmark
  selector:
    matchLabels:
      app: protobuf-server
  podMetricsEndpoints:
  - port: metrics
